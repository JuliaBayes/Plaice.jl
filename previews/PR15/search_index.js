var documenterSearchIndex = {"docs":
[{"category":"section","location":"#vector","page":"Plaice","text":"Plaice.jl converts samples from distributions to and from vectors.\n\nIt assumes that there are three forms of samples from a distribution d that we are interested in:\n\nThe original form, which is what rand(d) returns.\nA vectorised form, which is a vector that contains a flattened version of the original form.\nA unconstrained vectorised form, which is a vector in which:\neach element is independent; and\neach element is unconstrained (can take any value in ℝ).\n\nNote that because of the independence requirement, the unconstrained vectorised form may have a different dimension to the vectorised form. For example, when sampling from a Dirichlet distribution, the original form is a vector that always sums to 1. The unconstrained vectorised form will have one element less than the original form, because this constraint is eliminated.\n\nPlaice.jl provides functionality to convert between these three forms, via the following functions. Assuming that x = rand(d) for some distribution d:\n\nto_vec(d) is a function which converts x to the vectorised form\nfrom_vec(d) is the inverse of to_vec(d)\nvec_length(d) returns the length of to_vec(d)(x)\noptic_vec(d) returns a vector of optics that describes how each element of to_vec(d)(x) is accessed from x\nto_unconstrained_vec(d) is a function which converts x to the unconstrained vectorised form\nfrom_unconstrained_vec(d) is the inverse of to_unconstrained_vec(d)\nunconstrained_vec_length(d) returns the length of to_unconstrained_vec(d)(x)\nunconstrained_optic_vec(d) returns a vector of optics that describes how each element of to_unconstrained_vec(d)(x) is accessed from x (if possible)\n\nFor example:\n\njulia> using Plaice, Distributions\n\njulia> d = Beta(2, 2);\n       x = rand(d);  # x is between 0 and 1\n0.5602086057097567\n\njulia> to_vec(d)(x)\n1-element Vector{Float64}:\n 0.5602086057097567\n\njulia> to_unconstrained_vec(d)(x)\n1-element Vector{Float64}:\n 0.24200871395677753\n\nThe bijectors here also implement ChangesOfVariables.with_logabsdet_jacobian as well as InverseFunctions.inverse.","title":"Plaice"},{"category":"section","location":"#Implementing-your-own-vector-bijector","page":"Plaice","text":"The full Plaice interface consists of the following functions:\n\nIn practice, if your distribution is a univariate distribution, you will probably only need to implement scalar_to_scalar_bijector (see below).\n\nFor multivariate and matrix distributions, there are default implementations of the non-unconstrained versions (i.e., from_vec, to_vec, vec_length, and optic_vec) which should already be optimal. However you will have to define the unconstrained versions (see the examples page for more info).\n\nIf you have a very customised distribution, you will likely have to implement all the functions yourself.","title":"Implementing your own vector bijector"},{"category":"section","location":"#Known-constant-bijectors","page":"Plaice","text":"Additionally, if your distribution is likely to be used in part of a product distribution, it can lead to substantial performance improvements to overload has_constant_vec_bijector to return true (but make sure to only do this if the bijector is genuinely constant!):","title":"Known constant bijectors"},{"category":"section","location":"#Univariate-distributions","page":"Plaice","text":"For univariate distributions the default definition is to generate a bijector that inspects the minimum and maximum of the distribution. While this will work correctly, it might not be the most performant. You can manually define the Plaice API for univariate distributions, but it is probably faster to just overload the single function scalar_to_scalar_bijector: everything else will be automatically dervied.\n\nUnivariate distributions tend to fall into one of the following categories:","title":"Univariate distributions"},{"category":"section","location":"#Testing","page":"Plaice","text":"Because the scope of a vector bijector is very well-defined, there is a well-established testing framework to verify correctness of an implementation (Plaice.test_all()), which you can use in the test suite. This function contains additional keyword arguments to control the exact testing procedure. For example, you can test that the transformations do not cause extra allocations, should you know this to be the case for your bijector (note that this is not always possible).\n\nFor more information about generally testing bijectors (and in particular how to test Jacobians for transformations that modify the number of dimensions), see the examples page.\n\nOne of the most tricky parts of testing Plaice is ensuring that the transforms are compatible with automatic differentiation. This is important for DynamicPPL: we need to be able to compute the gradient of the log-density with respect to (possibly transformed) parameters, which may include the log-abs-det-Jacobian of the transformation. The default AD backends tested are ForwardDiff, ReverseDiff, Mooncake, and Enzyme. It is acceptable to skip tests for a particular backend if there are genuine upstream bugs, especially with ReverseDiff, which is not actively maintained. However where possible it is best to ensure that all backends are supported, and to use @test_broken to mark any known issues with specific backends.","title":"Testing"},{"category":"function","location":"#Plaice.from_vec","page":"Plaice","text":"Plaice.from_vec(dist)\n\nReturns a function that can be used to convert a vectorised sample from d back to its original form.\n\nfrom_vec(d) is the inverse of to_vec(d).\n\nExamples\n\njulia> using Plaice: from_vec; using Distributions\n\njulia> d = Beta(2, 2); from_vec(d)([0.5])\n0.5\n\njulia> d = product_distribution((a = Normal(), b = Beta(2, 2))); from_vec(d)([0.2, 0.5])\n(a = 0.2, b = 0.5)\n\n\n\n\n\n","title":"Plaice.from_vec"},{"category":"function","location":"#Plaice.to_vec","page":"Plaice","text":"Plaice.to_vec(dist)\n\nReturns a function that can be used to vectorise a sample from d.\n\nto_vec(d) is the inverse of from_vec(d).\n\nExamples\n\njulia> using Plaice: to_vec; using Distributions\n\njulia> d = Beta(2, 2); to_vec(d)(0.5)\n1-element Vector{Float64}:\n 0.5\n\njulia> d = product_distribution((a = Normal(), b = Beta(2, 2))); to_vec(d)((a = 0.2, b = 0.5))\n2-element Vector{Float64}:\n 0.2\n 0.5\n\n\n\n\n\n","title":"Plaice.to_vec"},{"category":"function","location":"#Plaice.from_unconstrained_vec","page":"Plaice","text":"Plaice.from_unconstrained_vec(dist)\n\nReturns a function that can be used to convert an unconstrained vector back to a sample from d.\n\nfrom_unconstrained_vec(d) is the inverse of to_unconstrained_vec(d).\n\nExamples\n\njulia> using Plaice: from_unconstrained_vec; using Distributions, Plaice\n\njulia> d = Beta(2, 2); from_unconstrained_vec(d)([1.0])\n0.7310585786300049\n\njulia> f = from_unconstrained_vec(d); with_logabsdet_jacobian(f, [1.0])\n(0.7310585786300049, -1.6265233750364456)\n\njulia> d = product_distribution((a = Normal(), b = Beta(2, 2))); from_unconstrained_vec(d)([0.2, 1.0])\n(a = 0.2, b = 0.7310585786300049)\n\njulia> f = from_unconstrained_vec(d); with_logabsdet_jacobian(f, [0.2, 1.0])\n((a = 0.2, b = 0.7310585786300049), -1.6265233750364456)\n\n\n\n\n\n","title":"Plaice.from_unconstrained_vec"},{"category":"function","location":"#Plaice.to_unconstrained_vec","page":"Plaice","text":"Plaice.to_unconstrained_vec(dist)\n\nReturns a function that can be used to convert a sample from d to an unconstrained vector.\n\nto_unconstrained_vec(d) is the inverse of from_unconstrained_vec(d).\n\nExamples\n\njulia> using Plaice: to_unconstrained_vec; using Distributions, Plaice\n\njulia> d = Beta(2, 2); to_unconstrained_vec(d)(0.5)\n1-element Vector{Float64}:\n 0.0\n\njulia> f = to_unconstrained_vec(d); with_logabsdet_jacobian(f, 0.5)\n([0.0], 1.3862943611198906)\n\njulia> d = product_distribution((a = Normal(), b = Beta(2, 2))); to_unconstrained_vec(d)((a = 0.2, b = 0.5))\n2-element Vector{Float64}:\n 0.2\n 0.0\n\njulia> f = to_unconstrained_vec(d); with_logabsdet_jacobian(f, (a = 0.2, b = 0.5))\n([0.2, 0.0], 1.3862943611198906)\n\n\n\n\n\n","title":"Plaice.to_unconstrained_vec"},{"category":"function","location":"#Plaice.vec_length","page":"Plaice","text":"Plaice.vec_length(dist)\n\nReturns the length of the vector representation of a sample from d, i.e., length(to_vec(d)(rand(d))). However, it does this without actually drawing a sample.\n\nExamples\n\njulia> using Plaice: vec_length; using Distributions\n\njulia> d = Beta(2, 2); vec_length(d)\n1\n\njulia> d = product_distribution((a = Normal(), b = Beta(2, 2))); vec_length(d)\n2\n\n\n\n\n\n","title":"Plaice.vec_length"},{"category":"function","location":"#Plaice.unconstrained_vec_length","page":"Plaice","text":"Plaice.unconstrained_vec_length(dist)\n\nReturns the length of the unconstrained vector representation of a sample from d, i.e., length(to_unconstrained_vec(d)(rand(d))). However, it does this without actually drawing a sample.\n\nExamples\n\njulia> using Plaice: unconstrained_vec_length; using Distributions\n\njulia> d = Beta(2, 2); unconstrained_vec_length(d)\n1\n\njulia> d = product_distribution((a = Normal(), b = Beta(2, 2))); unconstrained_vec_length(d)\n2\n\n\n\n\n\n","title":"Plaice.unconstrained_vec_length"},{"category":"function","location":"#Plaice.optic_vec","page":"Plaice","text":"Plaice.optic_vec(dist)\n\nReturns a vector of optics (from VarNames.jl), which describe how each element in the vectorised sample from d can be accessed from the original sample.\n\nFor example, if d = MvNormal(zeros(3), I), then optic_vec(d) would return a vector of [@opticof(_[1]), @opticof(_[2]), @opticof(_[3])]. The length of this vector would be equal to vec_length(d).\n\nIf optics = optic_vec(d), then for any sample x ~ d and its vectorised form v = to_vec(d)(x), it should hold that v[i] == optics[i](x) for all i.\n\nExamples\n\nFor the Beta case, the return value of an empty optic indicates that 'the first element of the vector is the sample itself'.\n\nFor the product_distribution case, the return value of Optic(.a) indicates that 'the first element of the vector is the .a component of the sample', and similarly for Optic(.b).\n\njulia> using Plaice: optic_vec; using Distributions\n\njulia> d = Beta(2, 2); optic_vec(d)\n1-element Vector{VarNames.Iden}:\n Optic()\n\njulia> d = product_distribution((a = Normal(), b = Beta(2, 2))); optic_vec(d)\n2-element Vector{VarNames.Property{_A, VarNames.Iden} where _A}:\n Optic(.a)\n Optic(.b)\n\n\n\n\n\n","title":"Plaice.optic_vec"},{"category":"function","location":"#Plaice.unconstrained_optic_vec","page":"Plaice","text":"Plaice.unconstrained_optic_vec(dist)\n\nReturns a vector of optics (from VarNames.jl), which describe how each element in the unconstrained vector representation of a sample from d is related to the original sample.\n\nThis is not always well-defined. For example, consider a Dirichlet distribution, with three components. The unconstrained vector representation would have two elements. However, these two elements do not correspond to specific components of the original sample, since all three components are interdependent (they must sum to one). In such cases, this function should return a vector of two nothings.\n\nHowever, for a distribution like MvNormal, this function would return a vector of [@opticof(_[1]), @opticof(_[2]), @opticof(_[3])], similar to optic_vec. That is because the first element of the unconstrained vector is solely determined by the first component of the original sample, the second element by the second component, and so on.\n\nNote that, unlike optic_vec, the first element of the unconstrained vector does not necessarily have to be exactly equal to the first component of the original sample, as there may have been a transformation applied. It merely needs to be determined by the first component (and only the first component).\n\nExamples\n\nIn this case since linking does not affect the 'provenance' of the sample (i.e., for the product distribution the first element of the unconstrained vector is still determined by .a and the second by .b), the return value of unconstrained_optic_vec is the same as that of optic_vec.\n\njulia> using Plaice: unconstrained_optic_vec; using Distributions\n\njulia> d = Beta(2, 2); unconstrained_optic_vec(d)\n1-element Vector{VarNames.Iden}:\n Optic()\n\njulia> d = product_distribution((a = Normal(), b = Beta(2, 2))); unconstrained_optic_vec(d)\n2-element Vector{VarNames.Property{_A, VarNames.Iden} where _A}:\n Optic(.a)\n Optic(.b)\n\n\n\n\n\n","title":"Plaice.unconstrained_optic_vec"},{"category":"function","location":"#Plaice.has_constant_vec_bijector","page":"Plaice","text":"Plaice.has_constant_vec_bijector(::Type{T}) where {T}\n\nReturn true if the vector bijector for each element of a collection of distributions is determined solely by the type of the distribution, and not by any runtime parameter values.\n\nThis is slightly confusing, so is best explained by example. Consider\n\nd = product_distribution(array_of_dists)\n\nIf it can be inferred from typeof(array_of_dists) that each distribution inside array_of_dists has the same vector bijector, then has_constant_vec_bijector(typeof(array_of_dists)) should return true.\n\nFor example, if array_of_dists is a FillArrays.Fill of some distribution type, then we know that each distribution inside is necessarily the same, and so they all have the same vector bijector. Thus, we have that\n\nhas_constant_vec_bijector(::Type{<:FillArrays.Fill}) == true\n\nFor generic AbstractArrays or Tuples, this function will recursively call itself on the element type of the array. That means that if a dist::D (where D<:Distribution) has a constant vector bijector, we can simply mark has_constant_vec_bijector(::Type{D}) == true, and that will automatically propagate to arrays of that distribution type.\n\nFor example, Beta has a constant vector bijector, because its support is always between 0 and 1, regardless of its parameters, and thus we can wite\n\nhas_constant_vec_bijector(::Type{<:Distributions.Beta}) == true\n\nOn the other hand, Uniform does not have a constant vector bijector, because its support depends on its parameters. So, it would be inappropriate (and can lead to wrong results) if we were to write has_constant_vec_bijector(::Type{<:Distributions.Uniform}) == true.\n\nwarning: Warning\nThis function must depend only on the type of the array of distributions or the\n\ndistribution, and not the actual value. This is necessary in order to guarantee type stability.\n\n\n\n\n\n","title":"Plaice.has_constant_vec_bijector"},{"category":"function","location":"#Plaice.scalar_to_scalar_bijector","page":"Plaice","text":"Plaice.scalar_to_scalar_bijector(dist)\n\nThe Plaice interface is intended to map samples to vectors. However, for univariate distributions, the 'vectorisation' part of this is trivial (we only need to convert a scalar to a vector of length one, and vice versa). Therefore, this function is provided to allow users to specify the 'interesting' part of the transformation, which is the function that maps values to unconstrained space.\n\nOverloading this function for a univariate distribution is sufficient to implement the entire Plaice interface for that distribution.\n\nThere are three scalar-to-scalar bijectors that are exported, which should be enough for any univariate distribution:\n\nPlaice.TypedIdentity\nPlaice.Log\nPlaice.Untruncate\n\nIf you need a different scalar-to-scalar bijector, please open an issue.\n\n\n\n\n\n","title":"Plaice.scalar_to_scalar_bijector"},{"category":"type","location":"#Plaice.TypedIdentity","page":"Plaice","text":"TypedIdentity <: ScalarToScalarBijector\n\nThe same as identity.\n\nThis is the appropriate scalar-to-scalar bijector for univariate distributions which have support over the entire real line. However, note that this can also be used in other contexts apart from univariate distributions. For example, TypedIdentity() is the correct implementation of to_vec and from_vec for MvNormal distributions.\n\nThe problem with using identity as a bijector is that ChangesOfVariables.jl defines with_logabsdet_jacobian(identity, x) = (x, zero(eltype(x))), which can fail if eltype(x) is not a number type! Implementing this allows us to shortcircuit that definition and return a sensible result (i.e. a Bool) even if x is not a numeric vector. Note that we intentionally do not return 0.0::Float64 in such cases as that can cause unwanted promotion.\n\n\n\n\n\n","title":"Plaice.TypedIdentity"},{"category":"type","location":"#Plaice.Log","page":"Plaice","text":"Log(bound, sign) <: ScalarToScalarBijector\n\nCallable struct, defined such that (l::Log)(x) = log(l.sign * (x - l.bound)). The sign is determined by the sign field.\n\nThis is the appropriate scalar-to-scalar bijector for univariate distributions which have a support of [l.bound, ∞) (if l.sign == 1), or (-∞, l.bound] (if l.sign == -1).\n\nwarning: Warning\nThis does not check whether the input is the domain of the transformation.\n\n\n\n\n\n","title":"Plaice.Log"},{"category":"type","location":"#Plaice.Untruncate","page":"Plaice","text":"Untruncate(a, b) <: ScalarToScalarBijector\n\nCallable struct, defined such that (::Untruncate(a, b))(x) maps a scalar x from (a, b) to (-Inf, Inf).\n\nThis is the appropriate scalar-to-scalar bijector for distributions which have support over (a, b).\n\nwarning: Warning\nThis does not check whether the input is a scalar in (a, b).\n\n\n\n\n\n","title":"Plaice.Untruncate"},{"category":"section","location":"example/#example","page":"Defining a vector bijector: an example","text":"Here we provide two different worked examples of defining a custom bijector.","title":"Defining a vector bijector: an example"},{"category":"section","location":"example/#Cyclic-permutation","page":"Defining a vector bijector: an example","text":"We start with something simple: a bijector that performs a cyclic permutation of the elements of a vector.\n\nusing Plaice\n\nstruct CircShift\n    shift::Int\nend\n\nThe most important function to implement is with_logabsdet_jacobian.\n\nLet's think for a moment about what the Jacobian is. CircShift is a mapping from ℝⁿ → ℝⁿ that permutes the elements of the input vector. For example, CircShift(1) would map a length-3 vector x = [x1, x2, x3] to y = [x3, x1, x2].\n\nThat means that the Jacobian matrix is\n\nJ = beginbmatrix\npartial y_1partial x_1  partial y_1partial x_2  partial y_1partial x_3 \npartial y_2partial x_1  partial y_2partial x _2  partial y_2partial x_3 \npartial y_3partial x_1  partial y_3partial x_2  partial y_3partial x_3\nendbmatrix\n= beginbmatrix\n0  0  1 \n1  0  0 \n0  1  0\nendbmatrix\n\nIn general, the Jacobian of such a transformation is a permutation matrix. The determinant of a permutation matrix is either 1 or -1, depending on whether the permutation is even or odd. (In this case, it is even; but it could be odd for other shifts and/or input sizes.) This means that the log-absolute determinant of the Jacobian is always 0.\n\nWe can now implement with_logabsdet_jacobian.\n\nfunction Plaice.with_logabsdet_jacobian(\n    b::CircShift,\n    x::AbstractVector{T},\n) where {T<:Number}\n    y = circshift(x, b.shift)\n    return y, zero(T)\nend\n\nnote: Note\nwith_logabsdet_jacobian actually belongs to the ChangesOfVariables.jl package, but Plaice.jl re-exports it so that you don't need to import it separately if Plaice is already a dependency.\n\nFirst of all, let's point out that we use Number as the type bound here instead of Real, which might be seen as more conventional. This is mainly for compatibility with Reactant.jl: Reactant's traced number types subtype Number but not Real.\n\nIt is good practice to let the type of the input determine the type of the log-Jacobian term here. However, you might also ask: since a cyclic permutation is also well-defined for arrays of non-real types, should we also allow that? We can do so by creating a new method, but we would have to make a choice as to the type of the log-Jacobian term, since we cannot derive it from the input type.\n\nWe could choose 0.0::Float64, but that is actually not the best idea: if the Float64 is added to another value, say a Float32, then the result will be promoted to a Float64, which is not ideal if we wanted to keep the result as a Float32. To avoid this, we choose the 'lowest' type in the promotion hierarchy, which is false::Bool.\n\nfunction Plaice.with_logabsdet_jacobian(b::CircShift, x::AbstractVector)\n    y = circshift(x, b.shift)\n    return y, false\nend\n\nCircShift should itself also be callable. In particular, we expect that (c::CircShift)(x) should perform the forward transformation.\n\nfunction (b::CircShift)(x::AbstractVector{T}) where {T<:Number}\n    return circshift(x, b.shift)\nend\n\nYou can implement this simply by calling first(with_logabsdet_jacobian(b, x)) to avoid code duplication, if there isn't a performance concern.\n\nFinally, we also define the inverse bijector:\n\nPlaice.inverse(b::CircShift) = CircShift(-b.shift)\n\nnote: Note\nSimilarly to with_logabsdet_jacobian, inverse is actually defined in the InverseFunctions.jl package, but Plaice.jl re-exports it.\n\nNow we can use the inverse bijector:\n\nb = CircShift(1)\nx = [1, 2, 3]\ny = b(x)\ninverse(b)(y) == x","title":"Cyclic permutation"},{"category":"section","location":"example/#Stereographic-projection","page":"Defining a vector bijector: an example","text":"Now, we'll look at a more complex example: a stereographic projection mapping points on the unit sphere (i.e., length-3 vectors x for which x_1^2 + x_2^2 + x_3^2 = 1), to points in the plane ℝ² (i.e., length-2 vectors y whose elements are unconstrained).\n\nThe relevant formulae are given here on Wikipedia. The forward transform (from sphere to plane) is:\n\ny_1 = fracx_11 - x_3 qquad y_2 = fracx_21 - x_3\n\nWe can implement this first:\n\nusing Plaice\n\nstruct StereographicProj end\nfunction (s::StereographicProj)(x::AbstractVector{T}) where {T<:Number}\n    y = similar(x, 2)\n    denom = one(T) - x[3]\n    y[1] = x[1] / denom\n    y[2] = x[2] / denom\n    return y\nend\n\nwarning: Warning\nThis will return [Inf, Inf] if x[3] == 1 (the 'north pole' of the sphere), which may potentially make downstream computations fail. One potential way around this is to add eps(T) to the denominator to avoid it ever being zero: you will sometimes see this trick used. However, be aware that the reverse transform has to also be modified accordingly so that the two transforms remain inverses of each other!\n\nWhen it comes to computing the Jacobian, we find ourselves in a spot of bother. The partial derivatives themselves can ostensibly be computed using fairly straightforward calculus:\n\nJ = beginbmatrix\npartial y_1partial x_1  partial y_1partial x_2  partial y_1partial x_3 \npartial y_2partial x_1  partial y_2partial x_2  partial y_2partial x_3\nendbmatrix\n= beginbmatrix\n1(1 - x_3)  0  x_1(1 - x_3)^2 \n0  1(1 - x_3)  x_2(1 - x_3)^2\nendbmatrix\n\nbut since our mapping is from ℝ³ → ℝ², the Jacobian matrix is not square, and so we cannot compute its determinant!\n\nTo fix this, we need to realise that x_1, x_2, and x_3 are not really independent at all. The partial derivatives we computed above treated them as independent variables! In reality, they must satisfy the constraint x_1^2 + x_2^2 + x_3^2 = 1, which means that\n\nx_3 = pm sqrt1 - x_1^2 - x_2^2\n\nand thus\n\nfracpartial x_3partial x_1 = -fracx_1x_3 qquad fracpartial x_3partial x_2 = -fracx_2x_3\n\n(Note that this is true regardless of which sign x_3 has.)\n\nIn effect, we are treating x_3 as a function of x_1 and x_2, rather than as an independent variable. This means that we can construct a Jacobian using only x_1 and x_2 as inputs, and thus obtain a square Jacobian matrix.\n\nFor example, we can recompute the derivative of y_1 with respect to x_1, but this time also making sure to include the dependence of x_3 on x_1.\n\nbeginalign*\nfracpartial y_1partial x_1\n= fracpartialpartial x_1 x_1(1 - x_3)^-1 \n= (1 - x_3)^-1 + x_1 (-1)(1 - x_3)^-2 left(fracpartialpartial x_1(1 - x_3)right) \n= (1 - x_3)^-1 - x_1 (1 - x_3)^-2 left(fracx_1x_3right) \n= frac11 - x_3 - fracx_1^2x_3 (1 - x_3)^2\nendalign*\n\nA similar strategy for all the other partial derivatives gives us the Jacobian\n\nJ = beginbmatrix\ndfrac11 - x_3 - dfracx_1^2x_3 (1 - x_3)^2  -dfracx_1 x_2x_3 (1 - x_3)^2 \n-dfracx_1 x_2x_3 (1 - x_3)^2  dfrac11 - x_3 - dfracx_2^2x_3 (1 - x_3)^2\nendbmatrix\n\nnote: Note\nWhen you see x_3 here, don't think 'the variable x_3': it's just shorthand for pm sqrt1 - x_1^2 - x_2^2. (And recall that these formulae hold for both choices of sign.)\n\nIts determinant very nicely simplifies to\n\ndet(J) = -frac1x_3 (1 - x_3)^2\n\nthe absolute determinant being\n\ndet(J) = frac1x_3 (1 - x_3)^2\n\n((1 - x_3)^2 is always non-negative, of course); and thus\n\nfunction Plaice.with_logabsdet_jacobian(\n    b::StereographicProj,\n    x::AbstractVector{T},\n) where {T<:Number}\n    logjac = -log(abs(x[3])) - (2 * log(one(T) - x[3]))\n    return b(x), logjac\nend\n\nPhew!\n\nLet's take a moment and check that we did indeed do this correctly. To verify that the implementation of the log-Jacobian is indeed correct, we can compare it against a Jacobian obtained via automatic differentiation.\n\nIf we try to calculate a Jacobian for StereographicProj(), we will just get a 2x3 matrix, which is not what we want. So, we need to take an extra step to map from the independent coordinates of x (i.e., x_1 and x_2) to the full 3D coordinates, and then to the plane:\n\nsgn = 1\n\nfunction full_transform(x12)\n    x3 = sgn * sqrt(one(eltype(x12)) - sum(x12 .^ 2))\n    x123 = vcat(x12, x3)\n    return StereographicProj()(x123)\nend\n\nimport DifferentiationInterface as DI\nusing FiniteDifferences, LinearAlgebra\nx = [0.3, 0.4, sgn * sqrt(1 - 0.3^2 - 0.4^2)]\n\nadtype = DI.AutoFiniteDifferences(; fdm=central_fdm(5, 1))\njac = DI.jacobian(full_transform, adtype, x[1:2])\nlogjac = logabsdet(jac)[1]\n\nHopefully this is approximately the same!\n\nlast(with_logabsdet_jacobian(StereographicProj(), x))\n\nYou can also rerun the code blocks above with sgn = -1 to verify that our log-Jacobian implementation does indeed behave correctly for both positive and negative values of x_3.\n\nWhen writing unit tests for a new bijector, it is a good idea to include comparisons like this to verify that the Jacobian is computed correctly. The strategy used above to get square Jacobians is quite generally applicable, and is used for testing the bijectors for (e.g.) simplices and Cholesky factors.\n\nNow, if we wanted to be more efficient, we might notice that one(T) - x[3] is computed both in s(x) as well as in the log-Jacobian term. So we could also write:\n\nfunction Plaice.with_logabsdet_jacobian(\n    s::StereographicProj,\n    x::AbstractVector{T},\n) where {T<:Number}\n    denom = one(T) - x[3]  # Shared computation\n    y = similar(x, 2)\n    y[1] = x[1] / denom\n    y[2] = x[2] / denom\n    logjac = -log(abs(x[3])) - (2 * log(denom))\n    return y, logjac\nend\n\nOf course, this alone is unlikely to save any meaningful amount of time, but other bijectors may have more expensive computations that may be shared between both transform and log-Jacobian calculations.\n\nThe inverse bijector can be implemented in a very similar way (Wikipedia has the formulae as well), but is left as an exercise for the very willing reader!\n\nFinally, suppose we had a distribution UnitSphere, where rand(UnitSphere()) returned a random point on the unit sphere. Something similar to this technically exists in Manifolds.jl, but we can also define a hacky version ourselves:\n\nusing Distributions\n\nstruct UnitSphere <: Distributions.ContinuousMultivariateDistribution end\nBase.size(::UnitSphere) = (3,)\nBase.rand(::UnitSphere) = normalize(rand(3))\n\nThen, we could define\n\nPlaice.to_unconstrained_vec(::UnitSphere) = StereographicProj()\nPlaice.unconstrained_vec_length(::UnitSphere) = 2\n\nand that would allow us to use UnitSphere as a distribution in a probabilistic programming language.","title":"Stereographic projection"}]
}
